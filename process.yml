apps:
  - script: ./src/index.js
    env:
      NODE_ENV: staging
    name: "api"
    instances: 1
    max_memory_restart: "1G"
    node_args: "--expose-gc --max-old-space-size=512"
    exec_mode: "cluster"
